const InsertImageButton = ({ onClick }) => {
    return (
        <div className="toolbar-button" onClick={onClick}>
            Insert Image
        </div>
    );
};

export default InsertImageButton;
